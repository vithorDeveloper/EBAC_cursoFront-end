const multiplicar = (a: number, b: number) : number => {return a * b};

console.log(multiplicar(2, 2));

const saudação = (nome: string) : string => {
  return `Olá ${nome}`
}

console.log(saudação('vitor'))